---
description: Run the one-time course environment setup (uv deps, kernel, smoke test, optional live notebook bridge).
---

Invoke the `course-setup` skill and follow it end to end. Treat any text after
the command as the unit number or preferences (e.g. "unit 2", "colab").

$ARGUMENTS
