.DS_Store
.venv/
__pycache__/
*.pyc
.ipynb_checkpoints/
*.egg-info/
dist/
build/
.pytest_cache/
.mypy_cache/

# Runtime data caches (OSM extracts fetched by demos — never commit)
*.pbf
*.osm

# Local-only secrets + state for the agent↔notebook bridge (never commit)
.env.local
.jupyter_ystore.db
